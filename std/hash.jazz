interface Hash {
    fun hash(): uint64;
}

interface Hasher {
    mut fun write_u8(val: uint8);
    mut fun write_u16(val: uint16);
    mut fun write_u32(val: uint32);
    mut fun write_u64(val: uint64);
    
    mut fun write_bytes(val: Array<uint8>);

    fun finish(): uint64;
}
