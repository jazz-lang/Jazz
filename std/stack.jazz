open "std/option.jazz"

__stack_proto__ = new(nil)
__stack_proto__.stack = anew()
__stack_proto__.__typename__ = "Stack"
__stack_proto__.push = |value| {
    apush(self.stack,value)
}
__stack_proto__.pop = |value| {
    if alen(self.stack) == 0 {
        none()
    } else {
        some(apop(self.stack))
    }
}

__stack_proto__.len = || {
    alen(self.stack)
}

__stack_proto__.arrray = || {
    self.stack
}