#[derive(Clone, PartialEq, Debug)]
#[repr(C)]
pub enum Opcode {
    AccNull,
    AccTrue,
    AccFalse,
    AccThis,
    AccInt(i64),
    AccFloat(f64),
    AccStr(String),
    AccStack(u32),
    AccStack0,
    AccStack1,
    AccStack2,
    AccGlobal(u32),
    AccEnv(u32),
    AccField(u64),
    AccArray,
    AccIndex(u32),
    AccBuiltin(u32),
    SetStack(u32),
    SetArray,
    SetField(u64),
    SetEnv(u32),
    SetGlobal(u32),
    SetIndex(u32),
    SetThis,
    Push,
    Pop(u16),
    Apply(u16),
    Call(u16),
    TailCall(u16),
    ObjCall(u16),
    Jump(u32),
    JumpIf(u32),
    JumpIfNot(u32),
    Ret(u16),
    MakeEnv(u32),
    MakeArray(u32),
    MakeArray2(u32),
    Bool,
    Not,
    IsNull,
    IsNotNull,
    Add,
    Sub,
    Mul,
    Div,
    Rem,
    Shl,
    Shr,
    UShr,
    Or,
    And,
    Xor,
    Eq,
    Neq,
    Lt,
    Lte,
    Gt,
    Gte,
    TypeOf,
    Cmp,
    PhysCompare,
    Hash,
    New,
    JumpTable,
    Last,
}

use std::fmt;
