inline func fac(x: i32) i32 {
	if x == 0 {
		return 1;
	} else {
		return fac(x - 1) * x;
	}
}

extern func printf(fmt: *char,...) void;

pub func main() i32 {
	printf("factorial(5) = %i\n",fac(5));

	return 0;
}