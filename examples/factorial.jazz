function rec factorial(x) {
  if x == 0 then 
    return 1
  else {
    return factorial(x - 1) * x
  }
}

console.log(factorial(5))

/* 
JavaScript code:
const rec = (le => ((f => f(f))(f => (le((...x) => f(f)(...x))))));
const factorial =  rec(factorial => ((x) => {
if (x == 0) {
return 1 } else {
return factorial(x - 1) * x

}

}));console.log(factorial(5));
*/